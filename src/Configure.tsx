import Theme from './components/Theme';
import Language from './components/Language';

const Configure = () => {
    return (
        <div>
            <Theme />
            <Language />
        </div>
    );
};

export default Configure;
